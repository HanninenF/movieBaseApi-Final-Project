export namespace AllDomEl {
  export const searchInput = document.querySelector(
    ".searchInput"
  ) as HTMLInputElement;
  export const dropdown = document.querySelector(
    "#categoryDropdown"
  ) as HTMLSelectElement;
}
