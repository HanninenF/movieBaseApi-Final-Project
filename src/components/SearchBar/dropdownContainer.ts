export const createDropdownContainer = (): HTMLDivElement => {
  const dropdownContainer = document.createElement("div");
  dropdownContainer.classList.add("dropdownContainer");
  return dropdownContainer;
};
